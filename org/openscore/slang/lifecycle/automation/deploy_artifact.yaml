namespace: slang.lifecycle.automation.flows
    imports:
      - ops: slang.lifecycle.automation.operations
    flow:
      name: Deploy_To_Artifactory
      inputs:
        - warFileLink
        - version
        - artifactoryRestUrl
      workflow:

