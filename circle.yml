machine:
  services:
    - docker
#
#dependencies:
#  cache_directories:
#    - "~/docker"
#
#  override:
#    - if [[ -e ~/docker/image.tar ]]; then docker load -i ~/docker/image.tar; fi
#    - docker pull jpetazzo/dind
#    - mkdir -p ~/docker; docker save jpetazzo/dind > ~/docker/image.tar

test:
  override:
    - docker run --privileged --lxc-conf="lxc.aa_profile=unconfined" -d -p 4444 -e PORT=4444 orius123/dind-ssh
    - wget https://github.com/CloudSlang/cloud-slang/releases/download/cloudslang-0.7.20/cslang-builder.zip
    - unzip cslang-builder.zip -d cslang-builder
    - chmod +x cslang-builder/bin/cslang-builder
    - ./cslang-builder/bin/cslang-builder .
